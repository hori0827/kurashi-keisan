# 自律収益プロジェクト — 運用ドクトリン

このファイルは**毎回の自動実行の起点**である。記憶を持たない状態で起動しても、
これを読めば今日やるべきことが確定するように書かれている。

## 目的

**月次収益 10万円**を、ユーザーの日常的な関与なしに達成し、維持する。
ユーザーの関与は「法律上人間にしかできないこと」だけに限定する（→ `SETUP_HUMAN.md`）。

### 10万円が意味する流入量（2026-08-06 実測ベースで算定）

金額は結果でしかない。**制御できるのは流入量**なので、常にこちらで考える。

| 収益源 | 月1万セッションあたり | 前提 |
|---|---|---|
| AdSense | 約 3,000円 | RPM 250円。ツールは1セッション1PVで離脱するため相場の下限 |
| アフィリエイト | 約 22,500円 | CVR 0.15%・単価1,500円（求人・派遣登録系） |
| 弱気ケース | 約 8,000円 | CVR 0.05%・単価1,000円 |

→ **月10万円 ≒ 月4〜6万セッション（1日1,500〜2,000）。弱気なら月12万セッション。**

この数字から逆算される事実を直視すること:

- **ツール3本では絶対に届かない。** 上位表示できたニッチツール1本の実力は
  月1,000〜10,000セッション。**10〜20本、または明確なヒット2〜3本が要る。**
- 「3本作る」はゴールではなく、**モデルが機能するかを見るための最小検証**にすぎない。
- 本数を増やすこと自体は目的ではない。**流入ゼロのツールを10本持っても収益はゼロ。**
  1本目が流入を取れないなら、2本目を作る前に原因を潰す。

### KPIラダー（この順に上がる。飛び級はない）

各段は**期限つき**。期限内に到達しなければ、次を作るのではなく**原因究明に切り替える**。

| 段 | 到達条件 | 期限の目安 | 未達なら |
|---|---|---|---|
| L0 | サイトが公開され、Search Console に登録済み | 公開経路の接続から3日 | 人間タスクを再掲・催促 |
| L1 | いずれかのツールが検索で表示される（表示回数 > 0） | 公開から4週 | インデックス／技術SEOを疑う |
| L2 | 月 1,000 セッション | 公開から3ヶ月 | **ニッチ選定を疑う。増産を止めて再選定** |
| L3 | 月 10,000 セッション／初の収益発生 | 公開から6ヶ月 | 収益導線かテーマ単価を疑う |
| L4 | 月 50,000 セッション／月10万円 | 公開から12〜18ヶ月 | — |

**L2で止まったら増産をやめる。** これが本ドクトリン最大の安全装置である。
流入の取れないツールを量産する状態は、成果が出ていないのに動き続けている状態であり、
最も損失が大きい（時間が溶け、しかも「やっている感」で失敗が隠れる）。

## 収益モデル（決定済み・議論を蒸し返さない）

**ニッチWebツールのポートフォリオ運営。**

- 実体は静的サイト（HTML/CSS/JS、クライアントサイド完結）。サーバー費用ゼロ。
- 1テーマ1ツール。無料で公開し、広告とアフィリエイトで回収する。
- 勝ったツールに追加投資し、負けたツールは放置（コストがゼロなので消さない）。

### なぜ記事量産ではないのか

Google のスパムポリシー「大規模コンテンツ不正使用」は、
**大量生成ページが検索評価を焼き切る**。記事farmは自滅する。
一方ツールは、それ自体が独自の機能価値を持つため該当しない。
共有もされる。SEOとSNSの両方に乗る。**この判断を覆さないこと。**

### 収益の柱（優先順）

1. **アフィリエイト** — 主力。単価が高く、少ない流入でも金額が立つ。
   月1万セッションあたり約2万円が期待値（CVR 0.15%・単価1,500円）。
   **申請の目安**: ツール2本＋月100セッション。ASP登録自体は実績不要だが、
   個別案件の提携審査は実績ゼロの `.pages.dev` だと落ちやすい。落ちたら再申請より
   **独自ドメイン取得（STEP 3-1）を先に**行うほうが速い。
2. **Google AdSense** — 補助。RPM 250円想定で、月1万セッションでも約3,000円にしかならない。
   **AdSense単独で10万円は33万PVが必要であり、事実上不可能**と理解しておくこと。
   **申請の目安**: ツール3本以上・各ページに実質的な機能と解説がある・月1,000セッション以上。
   「準備中」カードだけのページが残っている状態で申請すると
   「価値の低いコンテンツ」で不合格になり、再申請に時間を失う。
3. **有料機能** — 十分な流入が出た後に検討。それまで作らない。

## 絶対原則

- **正確さが商品**。計算結果が間違っているツールは価値がマイナス。
  数値・税率・制度は**必ず一次情報（官公庁サイト）で裏取り**してから実装する。
  記憶で書かない。出典をツール内に明記する。
- **制度には改定がある**。実装時に「根拠となる年度」と「出典URL」を
  `state/pipeline.json` に記録し、年度替わりに再検証する。
- **公開前ゲート**（必須・省略禁止）— `node scripts/verify.mjs` が **exit 0** を返すこと。
  以前は「ブラウザで目視確認」と書いていたが、**無人実行に目は無く、実行不能だった**。
  実行できない規則は毎晩破られ、憲法全体を形骸化させるので、機械が実行できる検査に
  置き換えてある。検査内容は、計算ロジックのテスト／YMYL必須表記／内部リンク切れ／
  h1・title・description／375pxでの横溢れ（puppeteer 導入時は実レンダリング）。
  - **各ツールに `*.test.mjs` を同居させること。テストの無いツールは公開できない**
    （`export default` で `{name, ok, expected, actual}` の配列を返す。境界値を必ず含む）
  - **ERROR が1件でもあれば push しない。** 通したくなったら基準を緩めるのではなく
    実装を直す。検査そのものを甘くする変更は `state/decisions.md` に理由を残すこと。
- **YMYL領域では断定しない**。税・保険・年金は「概算です。最終判断は
  税務署／専門家にご確認ください」を必ず併記する。
- **法規制に触れない**: 投資助言（金商法）、医療・健康の断定、
  転売、他者の著作物の転載は扱わない。
- **人間の承認が要る操作**は勝手に実行せず `SETUP_HUMAN.md` のキューに積む。
  具体的には: 金銭の支払い、アカウント新規作成、本人確認、外部への申請提出。
- **APIキーの従量課金を発生させない**。この自律ループは Claude Code の
  スケジュール実行で回す。Anthropic API を直接叩くコードは書かない。

## 日次ループ（自動実行時はこの順に実行する）

1. **状態を読む** — `state/pipeline.json` を読み、フェーズと未完了タスクを把握。
2. **前提ゲートを評価** — 下の「前提ゲート」を上から確認する。
   **該当した時点で本日の1手が確定する。意思決定ルールは見ない。**
3. **計測** — `py -3 scripts/fetch_metrics.py` を実行し、結果を `state/pipeline.json` の
   `metrics_history` に追記。
   計測基盤が未接続なら、**流入を「ゼロ」と記録してはならない**（`null` と記録する）。
4. **本日の1手を決める** — 「意思決定ルール」に従い、**1つだけ**選ぶ。
5. **実行する** — 公開前ゲートを通す。
6. **記録する** — `state/decisions.md` に「何を・なぜ・結果どうなる見込みか」を1段落。
   `state/pipeline.json` を更新。
7. **コミット** — 変更を git commit する。（push は認証設定後）

### 前提ゲート（2の判断基準・毎回最初に評価する）

打ち手を選ぶ前に上から確認し、**最初に該当したものが本日の1手になる。**

| 崩れている前提 | 本日の1手 |
|---|---|
| `git remote` が未設定（＝公開経路が無い） | **原則ツールを作らない。** `SETUP_HUMAN.md` の STEP 1 を最上部へ再掲し、詰まりを具体化する。ただし**未公開在庫が2本未満なら1本だけ**作ってよい |
| 未公開の在庫が **3本以上** ある | **絶対に作らない。** 在庫は1円も生まない。公開経路の解決のみに集中する |
| 計測基盤が未接続 | 計測の接続。人間作業が要るなら `SETUP_HUMAN.md` に手順を具体化して積む |
| L2（月1,000セッション）が**期限超過で未達** | **増産を停止。** 原因分析に切り替える（→ 意思決定ルール #2） |

**在庫上限3本の意味**: 公開できないまま作り続けるのは、需要の検証を1件もせずに
在庫を積む行為である。4本目を作る時間があるなら、公開経路が詰まっている理由を
1行でも具体的に書くほうが、10万円に近い。

### 意思決定ルール（4の判断基準）

前提ゲートに該当しなかった場合のみ、上から順に評価する。

| # | 条件 | 本日の1手 |
|---|---|---|
| 1 | 計測値が `null`（未計測） | 計測を通す。**未計測を「流入ゼロ」と解釈して施策を打つことを禁じる** |
| 2 | L2 未達のまま公開から3ヶ月経過 | **増産停止。** 順位・クエリ・競合ドメインを調べ、ニッチ選定の誤りを `research/niches.md` に反映 |
| 3 | 公開済みツールが3本未満 | 新規ツールを1本作る |
| 4 | 公開2週間以上・**表示回数0** | 技術SEO（インデックス登録・sitemap・構造化データ・内部リンク） |
| 5 | 表示はあるが平均掲載順位 > 20 | そのクエリに対する内容強化（不足している判定条件・説明の追加） |
| 6 | 流入はあるが収益ゼロ | 収益導線の追加（関連アフィリエイトの設置） |
| 7 | 収益が出ているツールがある | そのテーマの**周辺ツール**を作り面を取る |
| 8 | 上記なし | 新規ツールを1本作る |

**#4 と #5 は別物である。** 「表示回数0」はインデックスされていない技術問題、
「順位が低い」は内容の問題。混同すると効かない施策を打ち続けることになる。

## 週次レビュー（日曜・日次ループとは別物）

日次ループは「今日の1手」しか打たない。放っておくと**同じ判断を繰り返して空回りする**。
それを防ぐのが週次レビューの役目であり、打ち手ではなく**評価と方針修正**を行う。

1. この1週間の `state/decisions.md` を読み返す
2. ツールごとの流入・収益の実績を確認する
3. 「今のペースで月10万円に届くか。届かないならボトルネックは何か」に事実で答える
4. `facts_to_recheck` の期限が近い制度を一次情報で再検証する
5. **ホスト直下の `robots.txt` を確認する** — `https://hori0827.github.io/robots.txt`。
   これは別プロジェクト（HG Analytics）の管理下にあり、当サイトの巡回可否を握っている。
   2026-08-07 時点は `Allow: /`。`Disallow` が増えていたらインデックスが止まるので、
   その場合は独自ドメイン（STEP 3-1）への移行を最優先に切り替える
6. 結論を `state/decisions.md` に記録。**方針を変えるなら本ファイルを書き換えてよい**

**うまくいっていないときは、うまくいっていないと書くこと。**
成果を良く見せる記述は、将来の自分の判断を誤らせるので有害である。

### ツール選定のスコアリング

新規ツール候補は次の**7項目**を各5点で採点し、**合計24点以上（35点満点）**のみ着手する。

1. **検索意図の明確さ** — 「〇〇 計算」「〇〇 シミュレーター」で探される
2. **勝てる競合か** — ⚠ **ここを間違えやすい。** 「上位が古い」は弱さではない。
   古くても大手ドメインは勝つ。見るべきは**上位10件のドメインの正体**であり、
   個人・中小サイトが混じっていれば 4〜5点、
   **大手メディア・士業法人・公的機関で埋まっていたら 1点以下**
3. **クライアントサイドで完結するか** — サーバーが要るなら減点
4. **収益導線の有無** — 関連するアフィリエイト案件が存在するか。単価も見る
5. **正確に作れるか** — 一次情報が公開されており裏取りできる
6. **YMYL度の低さ**（低いほど高得点）— 税・保険・年金・投資・医療は **1〜2点**。
   これらは運営者が専門家・権威でない限り上位表示が難しい領域である。
   仕事の実務・生活の段取り・趣味・単位換算などは 4〜5点
7. **検索以外で配られるか** — SNSで共有される／人に教えたくなるか。
   新規ドメインは検索で勝つまで数ヶ月かかるため、**検索だけに賭けない**

**足切り（合計点に関わらず適用）**: 項目 **2 または 6 が 2点以下**の候補は、
**最初の3本には選ばない**。ドメイン評価がゼロの初期に、最も勝ちにくい土俵を
選ぶことになるため。実績が出てから戻ってくればよい。

**季節性を必ず併記する**。需要が特定月に集中するテーマ（年末調整・確定申告・
入園入学・年度替わり）は、**ピークの2〜3ヶ月前までに公開されていなければ
その年は捨てることになる**。着手判断に締切として書き込む。

採点結果は `research/niches.md` に残す。

## ディレクトリ

- `CLAUDE.md` … 本ファイル。運用の憲法
- `SETUP_HUMAN.md` … 人間にしかできない作業のキュー
- `state/pipeline.json` … 事業の現在地（機械可読）
- `state/decisions.md` … 意思決定ログ（なぜそうしたかの記録）
- `research/niches.md` … ツール候補と採点
- `docs/` … **公開される実体**。GitHub Pages の配信元
- `docs/tools/<slug>/` … 各ツール

## 公開の仕組み

**GitHub Pages。リポジトリは `hori0827/kurashi-keisan`、配信元は `main` ブランチの `/docs`。**
`main` に push すればそれだけで反映される。ビルドも外部サービスも要らない。

**公開URLは独自ドメイン**（2026-08-07 決定）。`state/pipeline.json` の
`site.custom_domain` が正であり、**このファイルにドメイン名を直書きしない**
（変わったときに2箇所を直す羽目になる）。

### 独自ドメインにした理由（2026-08-07 決定）

ユーザーの要望は「URLに自分の名前や数字を入れたくない」だった。
`hori0827.github.io/kurashi-keisan/` はアカウント名がそのままURLに出る。
独自ドメインはこれを解決すると同時に、**下の「プロジェクトサイトの制約」を3つとも消す**。

- ホストを HG Analytics（株式サイト）と共有しなくなる → 主題の混在が解消
- `robots.txt` を自分のホスト直下に置ける → sitemap の手動送信が不要
- サイトのルートがドメインのルートと一致する → パス設計の制約が消える

**運用上の必須事項**: `docs/CNAME` に取得ドメインを1行で置くこと。
これが無いと GitHub Pages はカスタムドメインを配信せず、**名前入りのURLのまま公開される**。
`node scripts/set_domain.mjs <ドメイン>` が pipeline / CNAME / sitemap / robots を
まとめて更新する。`verify.mjs` は CNAME の欠落と不一致を **ERROR** として検出する。

なお**リンクは相対パスのまま維持する**。独自ドメインならルート絶対パスでも動くが、
相対パスはどこに置いても動くので、わざわざ壊れやすいほうへ戻す理由がない。

### push と「公開」は別物である（2026-08-08 確認）

**GitHub Pages は Settings→Pages で発行元を設定するまで1枚も公開しない**
（[公式ドキュメント](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)）。
したがって:

- **リポジトリ作成と初回 push は、ドメイン取得を待つ必要がない。** 待たせていたのは
  2026-08-07 の設計ミスで、40秒の無料作業を10分の有料作業の後ろに並べていた。
  push はバックアップと経路確認になり、公開は始まらない
- **公開の引き金は Pages の有効化だけ。** `connect_github.ps1` は
  `site.custom_domain` が未設定のうちは Pages を有効化しない（名前入りURLで
  先に公開してしまうのを防ぐため）

### push が拒否されたときの正しい手順（`--force` の代わり）

**カスタムドメインを Settings→Pages で設定すると、GitHub が `CNAME` ファイルの
コミットをソースブランチに直接追加する**（[公式ドキュメント](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site/managing-a-custom-domain-for-your-github-pages-site)）。
このとき手元は遅れ、次の push は non-fast-forward で拒否される。
**これは異常ではなく正常な合流待ちである。**

- 正: `git fetch origin main` → `git pull --rebase origin main` → `git push`
- 誤: `git push --force`（相手側のコミットが消える。本ドクトリンで禁止）

`connect_github.ps1` はこの合流を自動で行い、競合したら `rebase --abort` して中止する。

### ⚠ `hori0827.github.io` は使えない（2026-08-07 判明）

**このリポジトリ名は既に埋まっている。** ユーザーは同じGitHubアカウントで
**別の本番サイト「HG Analytics」（株式スクリーニング）を運用しており**、
2026-05-09 開設・102コミット・Pages 有効の状態で稼働中である。

2026-08-06 の設計はこの事実を確認せずにユーザーサイトを前提に組まれていた。
その結果、初回 push は**毎朝失敗し続けていた**（`logs/run_2026-08-07.log`）。
しかもエラーメッセージは「リポジトリがまだ無いなら作ってください」と表示しており、
**真因と正反対の案内をしていた**。

**この事実から導かれる禁止事項（絶対に破らないこと）:**

- **`hori0827.github.io` に push しない。** 他人の本番サイトである
- **`git push --force` を使わない。** push が拒否されたときに `--force` を足すのは
  「解決」ではなく、**102コミットの本番サイトを消す操作**である。
  拒否されたら必ず原因を調べる。`scripts/connect_github.ps1` は
  push 先に既存コミットがあれば中止する安全装置を持っている。**外さないこと**
- ユーザーの他プロジェクトのファイルに触れない。リポジトリを共有しない

### プロジェクトサイトの制約と対処（対処済み）

プロジェクトサイトはサイトのルートがドメインのルートと一致しない。
そのため以前の設計が警戒していた問題が実際に発生する。**対処は完了している:**

- **ルート絶対パス（`/privacy.html` 等）は404する** → サイト内リンクを**全て相対パス**にした。
  `verify.mjs` がルート絶対リンクを **ERROR** として検出し、相対リンクの解決先も実在確認する。
  今後どのディレクトリに置いても壊れない
- **`robots.txt` はホスト直下のものしか読まれない** → `docs/robots.txt` は無視される。
  ホスト直下は HG Analytics の管理下（2026-08-07 時点は `Allow: /` で巡回は妨げられていない）。
  **sitemap は Search Console から直接送信する**。週次レビューでホスト側 robots.txt を確認する
- **ドメイン評価を株式サイトと共有する** → 主題が混ざる不利がある。
  これは**独自ドメイン（STEP 3-1）で解消できる**ため、STEP 3-1 を「任意」から
  **「L1到達後の推奨」**に格上げした

### なぜ Cloudflare Pages をやめたか（2026-08-06 変更）

当初は Cloudflare Pages を選んでいたが、**別アカウントの新規作成が必要**で、
それは人間にしかできない作業だった。GitHub Pages なら**リポジトリと同じアカウント**で
完結するため、人間の作業が1つ丸ごと消える。自律性が目的である以上こちらが正しい。
無料枠は帯域100GB/月・サイト1GBで、月10万円規模（月5万セッション）でも十分に余裕がある。

### なぜフォルダ名が `site` ではなく `docs` なのか

GitHub Pages が配信元に選べるのは **リポジトリのルート**か **`/docs`** だけだからである。
`site/` という名前にすると、GitHub Actions のワークフローか別ブランチへの
subtree push が要る。**自律実行の途中に壊れる部品を増やしたくない**ので、
GitHub の規約に合わせて `docs` にした。名前は妥協だが、可動部が減る。

### 公開先を決めるときに必ず確かめること

2026-08-06 に「ユーザーサイトなら絶対パスが動く」という理由で
`hori0827.github.io` を選んだが、**そのリポジトリ名が空いているかを確認していなかった**。
実際は埋まっており、この設計は最初から実行不可能だった。1日を失った。

**外部の資源を前提に設計するときは、実在と空きを実際に確認してから決める。**
名前・URL・アカウントは「たぶん空いている」で進めない。
確認は API 1回（`https://api.github.com/repos/<owner>/<repo>`）で済む。

## 環境メモ

- 実体パス: `C:\Users\horik_vle3kvw\OneDrive\Desktop\プロジェクト`（OneDrive配下）
- Python が要る場合は `py -3` を使う（Windowsストアのスタブは動かない）
- シェルは PowerShell。`&&` は使えない（`;` と `if ($?)` を使う）
- **`.ps1` は必ず UTF-8 BOM付きで保存する。** Windows PowerShell 5.1 は BOM の無い
  `.ps1` を ANSI(CP932) として読むため、日本語コメントのバイト列が壊れて
  **構文エラーで起動すらしない**。2026-08-06 にこれでランナー3本が全滅していた。
  スクリプトを書いたら `[System.Management.Automation.Language.Parser]::ParseFile()` で
  構文を確認するか、実際に実行して確かめること。**書いて終わりにしない。**
- **node_modules を作らない。** 本プロジェクトは OneDrive 配下にあり、
  数万ファイルの同期が git のファイルロックを誘発する。
  依存ゼロで書けないものは、OneDrive 外へ移すまで導入を見送る
