User-agent: *
Allow: /

# 注意: 本サイトは https://<login>.github.io/<repo>/ で配信されるため、
# このファイルは /<repo>/robots.txt に置かれ、クローラからは読まれない。
# 有効になるのは独自ドメインへ移行したとき（SETUP_HUMAN.md STEP 3-1）。
# それまで sitemap は Search Console から直接送信する。
# 公開URLが確定したら scripts/gen_sitemap.mjs が下行を書き換える。
# Sitemap: https://example.github.io/example/sitemap.xml
